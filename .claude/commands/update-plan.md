Update the IamSafe project plan at `A:/VibeCoding/ProductIdeas/IamSafe/PLAN.md` and the plan file at `A:\claude-data\plans\smooth-honking-yeti.md` to reflect the latest decisions.

Steps:
1. Read the current `PLAN.md` at `A:/VibeCoding/ProductIdeas/IamSafe/PLAN.md`
2. Read the current plan at `A:\claude-data\plans\smooth-honking-yeti.md`
3. Ask the user what has changed or what needs to be updated if not already clear from context
4. Update both plan files to reflect the latest decisions, keeping them in sync
5. Also update any relevant memory files in `A:/claude-data/projects/A--VibeCoding-ProductIdeas-IamSafe/memory/` if the stack or project decisions have changed
6. Confirm to the user that both plan files and memories are saved

Always preserve the full plan structure — only update the sections that changed. Never delete sections unless explicitly asked.
